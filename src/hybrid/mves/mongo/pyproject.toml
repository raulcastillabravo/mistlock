[project]
name = "mongo"
version = "0.1.0"
description = "Minimal viable example for MongoDB with Docker and MongoEngine"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mongoengine",
    "python-dotenv",
]

[dependency-groups]
dev = [
    "pytest",
]