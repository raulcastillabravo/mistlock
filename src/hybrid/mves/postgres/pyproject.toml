[project]
name = "postgres-docker"
version = "0.1.0"
description = "Minimal viable example for PostgreSQL with Docker and SQLAlchemy"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
  "sqlalchemy",
  "psycopg2-binary",
  "python-dotenv",
]

[dependency-groups]
dev = [
  "pytest",
]
