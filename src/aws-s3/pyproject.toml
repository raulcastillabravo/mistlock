[project]
name = "aws-s3"
version = "0.1.0"
description = "MVE emulating S3 with Garage for local data pipelines"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "python-dotenv",
    "pyarrow",
    "deltalake",
    "boto3",
]
