# Test bucket overrides. Connection keys come from .env (Garage regenerates
# them on each startup), so only the bucket names are overridden here.
S3_BUCKET_BRONZE=bronze-test
S3_BUCKET_SILVER=silver-test
