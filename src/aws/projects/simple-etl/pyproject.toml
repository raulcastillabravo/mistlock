[project]
name = "aws-dynamo-db"
version = "0.1.0"
description = "AWS DynamoDB example with LocalStack, S3, and Lambda"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "boto3",
    "python-dotenv",
]
