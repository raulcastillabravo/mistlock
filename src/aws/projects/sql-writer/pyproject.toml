[project]
name = "localstack-hybrid-cloud"
version = "0.1.0"
description = "Hybrid cloud example with LocalStack and external Postgres"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "boto3",
    "sqlalchemy",
    "psycopg2-binary",
    "python-dotenv",
]
