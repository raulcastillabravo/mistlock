[project]
name = "gcp-cloud-run"
version = "0.1.0"
description = "GCP Cloud Run with Firebase Emulator example"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "requests",
    "python-dotenv",
]
