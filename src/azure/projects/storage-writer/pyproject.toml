[project]
name = "azure-functions"
version = "0.1.0"
description = "Azure Functions local development with Azurite"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "azure-functions",
    "azure-storage-blob",
    "python-dotenv",
    "requests",
]
