[project]
name = "azure-sql-database"
version = "0.1.0"
description = "Azure SQL MVE with Dapr, Azurite and SQL Edge"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "dapr",
    "python-dotenv",
]
