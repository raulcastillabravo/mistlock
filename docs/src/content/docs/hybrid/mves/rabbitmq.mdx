---
title: RabbitMQ
description: This guide shows you how to work with RabbitMQ locally using Docker Compose and Python. This lab demonstrates real-time event publishing and consumption using the pika client.
isLab: true
---

import { Steps } from '@astrojs/starlight/components';
import { Tabs, TabItem } from '@astrojs/starlight/components';

This guide shows you how to work with **RabbitMQ** locally using **Docker Compose** and **Python**. This lab demonstrates real-time event publishing and consumption using the pika client.

## Prerequisites

- [Docker](https://www.docker.com/get-started) installed and running.
- [Dev Containers extension](vscode:extension/ms-vscode-remote.remote-containers) installed (optional).

## How to execute

<Steps>

1. **Setup Environment**:
   <Tabs>
      <TabItem label="Dev Container (recommended)">
         Open **VS Code** in the project folder and execute this command in the **Command Palette**:
         ```bash
         > Dev Containers: Reopen in Container
         ```
      </TabItem>
      <TabItem label="Manually">
         ```bash
         pip install uv && uv sync
         ```
      </TabItem>
   </Tabs>

2. **Start Infrastructure**: Launch the RabbitMQ container:
   ```bash
   docker compose up -d
   ```

3. **Run the Example**:
   1. **Publish events** (Terminal 1):
      ```bash
      python publish.py
      ```
   2. **Listen for events** (new terminal):
      ```bash
      python listen.py
      ```

</Steps>

## Validate results

**Open** the RabbitMQ Management UI and navigate to **Queues** to inspect the `user_events` queue (credentials from `.env`):
```bash
http://localhost:15672
```

## Clean Up

```bash
docker compose down -v
```
