---
title: Airflow
description: Esta guía te muestra cómo trabajar con Apache Airflow localmente usando Docker. Este lab demuestra un pipeline ETL simple con tareas de extracción, transformación y carga ejecutadas como un DAG programado.
isLab: true
---

import { Steps } from '@astrojs/starlight/components';
import { Tabs, TabItem } from '@astrojs/starlight/components';

Esta guía te muestra cómo trabajar con **Apache Airflow** localmente usando **Docker**. Este lab demuestra un pipeline ETL simple con tareas de extracción, transformación y carga ejecutadas como un DAG programado.

## Prerrequisitos

- [Docker](https://www.docker.com/get-started) instalado y en ejecución.

## Cómo ejecutar

<Steps>

1. **Instala las dependencias**:
   ```bash
   pip install uv && uv sync
   ```

2. **Crea** los directorios necesarios:
   ```bash
   mkdir -p dags logs plugins
   ```

2. **Iniciar la infraestructura**: Lanza todos los servicios de Airflow:
   ```bash
   docker compose up -d
   ```

3. **Activar el DAG**:
   - **Abre** la UI de Airflow en tu navegador (credenciales en `.env`):
     ```bash
     http://localhost:8080
     ```
   - **Activa** el DAG `etl_pipeline` haciendo clic en el interruptor. El DAG se ejecuta automáticamente cada 30 segundos.

</Steps>

## Validar resultados
- **UI de Airflow**:
   - **Monitoriza**: Haz clic en el nombre del DAG para ver el grafo (`extract_task` → `transform_task` → `load_task`).
   - **Logs**: Haz clic en una caja de tarea, selecciona la instancia de la tarea y haz clic en **Log** para inspeccionar la salida del ETL.
- **Inspecciona los archivos generados**: los archivos creados dentro del contenedor worker:
   ```bash
   docker exec -it <worker_container_id> ls -la storage/extract/
   docker exec -it <worker_container_id> ls -la storage/transform/
   docker exec -it <worker_container_id> ls -la storage/load/
   ```

## Limpieza

```bash
docker compose down -v
rm -rf logs/*
```

## Solución de problemas

| Problema | Solución |
|----------|----------|
| Los servicios no arrancan | Revisa los logs con `docker compose logs`. La inicialización puede tardar 30-60 segundos. |
| El DAG no aparece | Asegúrate de que los archivos están en `dags/`, comprueba si hay errores de Python y recarga la UI. |
