---
title: Firebase Cloud Functions
description: "Run Firebase Cloud Functions locally with the Firebase Emulator Suite: a synchronous HTTPS function that reads secrets from Secret Manager."
isLab: true
---

import { Steps } from '@astrojs/starlight/components';
import { Tabs, TabItem } from '@astrojs/starlight/components';

This guide shows you how to work with **Google Cloud Firebase Functions** locally using the **Firebase Emulator Suite**. This lab demonstrates a synchronous HTTPS function that retrieves a secret from Secret Manager based on user name.

## Prerequisites

- [Docker](https://www.docker.com/get-started) installed and running.
- [Dev Containers extension](vscode:extension/ms-vscode-remote.remote-containers) installed (optional).

## How to execute

<Steps>

1. **Setup Environment**:
   <Tabs>
      <TabItem label="Dev Container (recommended)">
         Open **VS Code** in the project folder and execute this command in the **Command Palette**:
         ```bash
         > Dev Containers: Reopen in Container
         ```
      </TabItem>
      <TabItem label="Manually">
         **Run the setup** script to install tools and dependencies:
         ```bash
         scripts/setup.sh
         ```
      </TabItem>
   </Tabs>

2. **Start Firebase Emulators**:
   ```bash
   firebase emulators:start
   ```

3. **Run the Example**:
   <Tabs>
      <TabItem label="Python">
         ```bash
         python main.py
         ```
      </TabItem>
      <TabItem label="cURL">
         ```bash
         curl "http://localhost:5001/demo-mve-firebase-functions/us-central1/get_secret?username=admin"
         ```
      </TabItem>
      <TabItem label="REST Client">
         - **Install**: [REST Client](vscode:extension/humao.rest-client) from de VS Code Marketplace. 
         - **Open**: Open `http/get_secret.http` in VS Code.
         - **Run**: Click **Send Request** above any request block.
      </TabItem>
   </Tabs>

</Steps>

## How to debug

1. **Cloud Function**:
   - **Ensure**: Stop the Firebase Emulator before attaching the debugger (it conflicts with the debugger port).
   - **Run**: Launch the **Debug Cloud Function (Local)** configuration from the **Run and Debug** tab.
   - **Breakpoints**: Set breakpoints inside `get_secret` in `functions/main.py`.
   - **Send**: Trigger a request via **curl** or the REST Client pointing to `http://localhost:8080/`.

2. **main.py**:
   - **Ensure**: Stop the Firebase Emulator before attaching the debugger.
   - **Run**: Launch the **Debug Cloud Function (Local)** configuration.
   - **Set**: Set `DEBUG_MODE=True` in your `.env`.
   - **Run**: Open `main.py` and start the **Python: Current File** debugger.

3. **Tests**:
   - **Ensure**: The Firebase Emulator **must be running** for individual test debugging.
   - **Breakpoints**: Set breakpoints in `tests/test_get_secret.py`.
   - **Run**: Open the **Testing** tab and click the **Debug Test** icon next to the desired test.

## How to test

- **All tests**: Run the automated script:
  ```bash
  scripts/run_tests.sh
  ```
- **Individually**: Use the VS Code **Testing** tab to run or debug specific test cases.

:::note[Firebase Emulator and tests]
`scripts/run_tests.sh` starts its own emulator via `firebase emulators:exec`. Stop any running emulator before executing it.
:::

## Validate results

**Open** the Firebase Emulator UI and navigate to the **Logs** section to inspect function activity:
```bash
http://localhost:4000
```

## Clean Up

Stop the emulators by pressing `Ctrl+C` in the terminal running `firebase emulators:start`. To remove volumes:

```bash
docker compose down -v
```

## Next steps

- [Build on this MVE with the Storage Writer project.](/google-cloud/projects/storage-writer/)
- [Build on this MVE with the Simple ETL project.](/google-cloud/projects/simple-etl/)
- [Build on this MVE with the No-SQL Writer project.](/google-cloud/projects/no-sql-writer/)
