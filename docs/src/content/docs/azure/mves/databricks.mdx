---
title: Databricks
description: Simulate Databricks locally with Docker, MinIO, and PostgreSQL as Hive Metastore to develop and test Spark and Delta Lake ETLs.
isLab: true
---

import { Steps } from '@astrojs/starlight/components';
import { Tabs, TabItem } from '@astrojs/starlight/components';

This guide shows you how to simulate a **Databricks** environment locally using **Docker**, **MinIO** as S3-compatible storage, and **PostgreSQL** as a persistent Hive Metastore. This lab demonstrates how to develop and test Spark and Delta Lake ETLs locally using **Databricks Runtime 15.4 LTS (Spark 3.5.0)**.

## Prerequisites

- [Docker](https://www.docker.com/get-started) installed and running.
- [Dev Containers extension](vscode:extension/ms-vscode-remote.remote-containers) installed (optional).

## How to execute

<Steps>

1. **Setup Environment**:
   <Tabs syncKey="environment">
      <TabItem label="Dev Container (recommended)">
         Open **VS Code** in the project folder and execute this command in the **Command Palette**:
         ```bash
         > Dev Containers: Reopen in Container
         ```
      </TabItem>
      <TabItem label="Manually">
         1. **Start Infrastructure**: Launch all required services (Spark, MinIO, PostgreSQL).
            ```bash
            docker compose up -d
            ```
         2. **Run the setup** script inside the Spark container to install tools and dependencies.
            ```bash
            docker compose exec spark bash scripts/setup.sh
            ```
      </TabItem>
   </Tabs>

2. **Run the Example**:
   <Tabs syncKey="environment">
      <TabItem label="Dev Container (recommended)">
         ```bash
         python main.py
         ```
      </TabItem>
      <TabItem label="Manually">
         Execute the script inside the Spark container:
         ```bash
         docker compose exec spark .venv/bin/python main.py
         ```
      </TabItem>
   </Tabs>

</Steps>

:::note[Interactive Analysis (Dev Container only)]
Open `src/notebooks/analysis.ipynb` and run the cells to analyze the data registered in the Hive Metastore and test the DBUtils Shim (secrets and widgets). This requires the pre-configured Jupyter environment provided by the Dev Container.
:::

## How to debug

1. **Python Script (main.py)**:
   - **Open** `main.py` and set breakpoints.
   - **Run** the **Python: Main** configuration from the **Run and Debug** tab.
2. **Interactive Notebook**:
   - **Open** `src/notebooks/analysis.ipynb` and debug cells with the Jupyter debugger.

## How to test

- **All tests**: Run the automated script: `scripts/run_tests.sh`
- **Individually**: Use the VS Code **Testing** tab to run or debug specific test cases.

## Validate results

<Tabs>
   <TabItem label="Spark UI">
      While a job is running, **Open** the Spark UI:
      ```bash
      http://localhost:4040
      ```
   </TabItem>
   <TabItem label="MinIO Console">
      **Browse** the Delta files written to the bucket. Log in with `MINIO_ROOT_USER` / `MINIO_ROOT_PASSWORD`:
      ```bash
      http://localhost:9001
      ```
   </TabItem>
</Tabs>

## Clean Up

```bash
docker compose down -v
```

## Troubleshooting

| Issue | Solution |
|-------|----------|
| Connection Refused | Ensure all services are running: `docker compose ps` |
| Table already exists | Drop the conflicting table with `spark.sql("DROP TABLE IF EXISTS sales.products_silver")` or run **Clean Up** to reset the volumes. |

## Next steps

- [Explore the related S3 (MinIO) Lab.](/aws/mves/s3-minio/)
- [Explore the related PostgreSQL Lab.](/hybrid/mves/postgres/)
