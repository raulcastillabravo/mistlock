---
title: Blob Storage
description: This guide shows you how to work with Azure Blob Storage locally using Azurite, Docker Compose, and Python. This lab demonstrates how to create containers and upload and download blobs.
isLab: true
---

import { Steps } from '@astrojs/starlight/components';
import { Tabs, TabItem } from '@astrojs/starlight/components';

This guide shows you how to work with **Azure Blob Storage** locally using **Azurite**, **Docker Compose**, and **Python**. This lab demonstrates how to create containers and upload and download blobs.

## Prerequisites

- [Docker](https://www.docker.com/get-started) installed and running.
- [Dev Containers extension](vscode:extension/ms-vscode-remote.remote-containers) installed (optional).
- [Azure Storage extension](vscode:extension/ms-azuretools.vscode-azurestorage) (optional, included in Dev Container).

## How to execute

<Steps>

1. **Setup Environment**:
   <Tabs>
      <TabItem label="Dev Container (recommended)">
         Open **VS Code** in the project folder and execute this command in the **Command Palette**:
         ```bash
         > Dev Containers: Reopen in Container
         ```
      </TabItem>
      <TabItem label="Manually">
         ```bash
         pip3 install uv && uv sync
         ```
      </TabItem>
   </Tabs>

2. **Start Infrastructure**: Launch the Azurite container.
   ```bash
   docker compose up -d
   ```

3. **Run the Example**:
   ```bash
   python main.py
   ```

</Steps>

## Validate results

**Check using [Azure Storage extension](https://marketplace.visualstudio.com/items?itemName=ms-azuretools.vscode-azurestorage)**:
- **Open**: Open the **Azure** view in VS Code (Azure icon in the sidebar).
- **Connect**: Under **Storage Accounts**, click **Attach Storage Account** > **Attach to a local emulator**.
- **Browse**: Use the default Azurite connection settings to browse your containers and blobs visually.


## Clean Up

```bash
docker compose down -v
```

## Troubleshooting

| Issue | Solution |
|-------|----------|
| Port 10000 in use | Update the port mapping in `docker-compose.yml` and `BlobEndpoint` in `.env`. |
| Connection Refused | Ensure Azurite is running: `docker ps` |
| Module Not Found | Reinstall dependencies: `pip3 install uv && uv sync` |
